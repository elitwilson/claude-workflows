# config.toml
# ------------------------------------------------------------
# User Plugin Configuration for Claude Workflows Plugin
# Access values in your plugin via `ctx.config.<key>`

# GitHub repository configuration
repo_url = "https://raw.githubusercontent.com/elitwilson/claude-workflows"
default_branch = "main"
